# Example moved

Runnable Playwright samples live in the monorepo root:

**[`example/playwright`](../../../example/playwright)**

- `tests/signup-otp.spec.ts` — OTP signup
- `tests/verify-link.spec.ts` — magic / verify link
- `tests/manual-inbox.spec.ts` — `autoCreate: false`

See that directory's README for setup (`pnpm dev` + `CHOBITMAIL_API_KEY`).
